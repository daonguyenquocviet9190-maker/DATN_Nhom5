"use client";
import { useEffect, useMemo, useState } from "react";
import { Loader2, Percent, Search, TicketPercent } from "lucide-react";
import { extractItems, getAdminPromotions } from "@/services/admin.service";
import { formatCurrency } from "@/data/shop";

export default function AdminPromotionsPage(){const[items,setItems]=useState([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{async function load(){try{setLoading(true);const res=await getAdminPromotions({per_page:200});setItems(extractItems(res,["promotions","coupons"]));}catch(err){setError(err?.message||"Không thể tải mã giảm giá. Cần API /api/admin/promotions.");setItems([])}finally{setLoading(false)}}load()},[]);const filtered=useMemo(()=>{const k=query.trim().toLowerCase();return items.filter(i=>!k||[i.code,i.name,i.description].filter(Boolean).join(' ').toLowerCase().includes(k))},[items,query]);return <div className="space-y-6"><section className="rounded-[32px] border border-white/10 bg-white/[0.06] p-6 backdrop-blur-xl"><p className="text-xs font-black uppercase tracking-[0.22em] text-orange-300">Promotions</p><h2 className="mt-2 text-2xl font-black text-white">Quản lý mã giảm giá</h2><p className="mt-1 text-sm font-semibold text-slate-500">Danh sách mã giảm giá từ API.</p><div className="mt-5 flex items-center gap-3 rounded-2xl border border-white/10 bg-white/[0.05] px-4 py-3 text-slate-400"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} className="w-full bg-transparent text-sm font-semibold text-white outline-none placeholder:text-slate-500" placeholder="Tìm mã giảm giá..."/></div></section>{error&&<div className="rounded-3xl border border-rose-500/20 bg-rose-500/10 p-4 text-sm font-bold text-rose-200">{error}</div>}<section className="rounded-[32px] border border-white/10 bg-white/[0.06] p-5 backdrop-blur-xl">{loading?<div className="grid h-72 place-items-center"><Loader2 className="animate-spin text-orange-300" size={34}/></div>:filtered.length===0?<div className="grid h-72 place-items-center text-center"><div><TicketPercent className="mx-auto text-orange-300" size={42}/><p className="mt-4 font-black text-white">Chưa có mã giảm giá</p></div></div>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map((item,index)=><div key={item.id||index} className="rounded-[26px] border border-white/10 bg-white/[0.04] p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xl font-black text-orange-300">{item.code||item.name}</p><p className="mt-1 text-sm text-slate-500">{item.description||'Mã khuyến mãi'}</p></div><Percent className="text-orange-300"/></div><div className="mt-5 grid gap-2 text-sm font-bold text-slate-400"><p>Giá trị: <span className="text-white">{item.type==='percent'||item.discount_type==='percent'?`${item.value||item.discount_value}%`:formatCurrency(item.value||item.discount_value||0)}</span></p><p>Đơn tối thiểu: <span className="text-white">{formatCurrency(item.min_order||item.min_order_value||0)}</span></p><p>Trạng thái: <span className="text-emerald-300">{item.status||'active'}</span></p></div></div>)}</div>}</section></div>}
